extends layout
block content
    // preset form values if we receive a userdata object //
    - user = typeof(udata) != 'undefined' ? udata : { }
    // store the userId on the client side in a hidden input field //
    input(type='hidden', value= user._id)#userId

    #account-form-container
        form(method="post",name="signup-form",onsubmit="return validateForm();")#account-form.form-horizontal.well.span6
            h1 Register with Us!!
            p#sub1.subheading Please tell us a little about yourself.
            hr
            fieldset
                #name-cg.control-group
                    label.control-label(for='name-tf') Name
                    .controls
                        input#name-tf.input-xlarge(type='text', name='name', value= user.name)
                #email-cg.control-group
                    label.control-label(for='email-tf') Email
                    .controls
                        input#email-tf.input-xlarge(type='text', name='email', value= user.email)
                hr
                p#sub2.subheading
                #user-cg.control-group
                    label.control-label(for='user-tf') Username
                    .controls
                        input#user-tf.input-xlarge(type='text', name='user', value= user.user)
                #pass-cg.control-group
                    label.control-label(for='pass-tf') Password
                    .controls
                        input#pass-tf.input-xlarge(type='password', name='pass', value='')
                h3 #{message}
                .form-actions
                    button(type='button',onClick="history.go(-1);return true;")#account-form-btn1.btn.well.span1 cancel
                    button(type='submit')#account-form-btn2.btn.btn-primary.well.span1 Register
block scripts
     script(src='/javascripts/SignUpFormValidator.js')